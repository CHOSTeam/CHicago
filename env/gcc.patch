diff -uarN a/config.sub b/config.sub
--- a/config.sub	2020-05-07 07:49:58.000000000 -0300
+++ b/config.sub	2020-06-16 22:18:38.943705300 -0300
@@ -1343,7 +1343,7 @@
 	     | hpux* | unos* | osf* | luna* | dgux* | auroraux* | solaris* \
 	     | sym* | kopensolaris* | plan9* \
 	     | amigaos* | amigados* | msdos* | newsos* | unicos* | aof* \
-	     | aos* | aros* | cloudabi* | sortix* \
+	     | aos* | aros* | cloudabi* | sortix* | chicago* \
 	     | nindy* | vxsim* | vxworks* | ebmon* | hms* | mvs* \
 	     | clix* | riscos* | uniplus* | iris* | isc* | rtu* | xenix* \
 	     | knetbsd* | mirbsd* | netbsd* \
diff -uarN a/fixincludes/mkfixinc.sh b/fixincludes/mkfixinc.sh
--- a/fixincludes/mkfixinc.sh	2020-05-07 07:49:59.000000000 -0300
+++ b/fixincludes/mkfixinc.sh	2020-06-16 22:19:12.447752100 -0300
@@ -11,8 +11,10 @@
 
 # Check for special fix rules for particular targets
 case $machine in
+    i?86-*-chicago* | \
     i?86-*-cygwin* | \
     i?86-*-mingw32* | \
+    x86_64-*-chicago* | \
     x86_64-*-mingw32* | \
     powerpc-*-eabisim* | \
     powerpc-*-eabi*    | \
diff -uarN a/gcc/config/chicago.h b/gcc/config/chicago.h
--- a/gcc/config/chicago.h	1969-12-31 21:00:00.000000000 -0300
+++ b/gcc/config/chicago.h	2020-06-16 22:20:05.569938800 -0300
@@ -0,0 +1,34 @@
+#undef TARGET_CHICAGO
+#define TARGET_CHICAGO 1
+
+#undef STANDARD_STARTFILE_PREFIX
+#define STANDARD_STARTFILE_PREFIX "/Development/Libraries/"
+
+#undef LIB_SPEC
+#define LIB_SPEC "-lc" /* link everything against the C standard library. */
+
+#undef LIBSTDCXX
+#define LIBSTDCXX "cxx" /* CHicago's c++ library is called libcxx (the gcc's default is called libstdc++). */
+
+/* Files that are linked before user code.
+   The %s tells GCC to look for these files in the library directory. */
+#undef STARTFILE_SPEC
+#define STARTFILE_SPEC "crti.o%s %{shared|!no-pie:crtbeginS.o%s;:crtbegin.o%s} crt0.o%s"
+
+/* Files that are linked after user code. */
+#undef ENDFILE_SPEC
+#define ENDFILE_SPEC "%{shared|!no-pie:crtendS.o%s;:crtend.o%s} crtn.o%s"
+
+#undef LINK_SPEC
+#define LINK_SPEC "%{shared:-shared} %{static:-static} %{!shared: %{!static: -dynamic-linker /System/Boot/chkrnl.elf %{rdynamic:-export-dynamic}}} -z max-page-size=4096"
+
+#undef CC1_SPEC
+#define CC1_SPEC "%{cc1_cpu} -fno-plt"
+
+/* Don't automatically add extern "C" { } around header files. */
+#undef  SYSTEM_IMPLICIT_EXTERN_C
+#define SYSTEM_IMPLICIT_EXTERN_C 0
+
+/* Additional predefined macros. */
+#undef TARGET_OS_CPP_BUILTINS
+#define TARGET_OS_CPP_BUILTINS() do { builtin_define ("__chicago__"); builtin_assert ("system=chicago"); } while(0);
diff -uarN a/gcc/config.gcc b/gcc/config.gcc
--- a/gcc/config.gcc	2020-05-07 07:49:59.000000000 -0300
+++ b/gcc/config.gcc	2020-06-16 22:21:38.517188400 -0300
@@ -675,6 +675,14 @@
 
 # Common parts for widely ported systems.
 case ${target} in
+*-*-chicago*)
+  gas=yes
+  gnu_ld=yes
+  default_use_cxa_atexit=yes
+  use_gcc_stdint=wrap
+  native_system_header_dir=/Development/Headers
+# Empty line only so the patch doesn't fail out with the "malformed patch" error, I need to recreate this patch file later... and stop editing it manually...
+  ;;
 *-*-darwin*)
   tmake_file="t-darwin "
   tm_file="${tm_file} darwin.h"
@@ -1838,6 +1846,12 @@
 		dwarf2=no
 	fi
 	;;
+i[34567]86-*-chicago*)
+	tm_file="${tm_file} i386/unix.h i386/att.h dbxelf.h elfos.h glibc-stdint.h i386/i386elf.h chicago.h"
+	;;
+x86_64-*-chicago*)
+	tm_file="${tm_file} i386/unix.h i386/att.h dbxelf.h elfos.h glibc-stdint.h i386/i386elf.h i386/x86-64.h chicago.h"
+	;;
 i[34567]86-*-darwin1[89]*)
 	echo "Error: 32bit target is not supported after Darwin17" 1>&2
 	;;
diff -uarN a/libgcc/config.host b/libgcc/config.host
--- a/libgcc/config.host	2020-05-07 07:50:02.000000000 -0300
+++ b/libgcc/config.host	2020-06-16 22:22:29.206414600 -0300
@@ -684,6 +684,14 @@
 hppa*-*-netbsd*)
 	tmake_file="$tmake_file pa/t-netbsd"
 	;;
+i[34567]86-*-chicago*)
+	extra_parts="$extra_parts crti.o crtbegin.o crtbeginS.o crtend.o crtendS.o crtn.o"
+	tmake_file="$tmake_file i386/t-crtstuff t-crtstuff-pic t-libgcc-pic"
+	;;
+x86_64-*-chicago*)
+	extra_parts="$extra_parts crti.o crtbegin.o crtbeginS.o crtend.o crtendS.o crtn.o"
+	tmake_file="$tmake_file i386/t-crtstuff t-crtstuff-pic t-libgcc-pic"
+	;;
 i[34567]86-*-darwin*)
 	tmake_file="$tmake_file i386/t-crtpc t-crtfm i386/t-msabi"
 	tm_file="$tm_file i386/darwin-lib.h"
